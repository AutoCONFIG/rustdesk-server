--- a/src/database.rs
+++ b/src/database.rs
@@ -38,12 +38,9 @@ pub struct Database {
 #[derive(Default)]
 pub struct Peer {
     pub guid: Vec<u8>,
-    pub id: String,
     pub uuid: Vec<u8>,
     pub pk: Vec<u8>,
-    pub user: Option<Vec<u8>>,
     pub info: String,
-    pub status: Option<i64>,
 }
 
 impl Database {
